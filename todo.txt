jobs:

Bugs:

standart project startet bei 2? 
es wird rechts immer das standart projekt angezeigt.
=> Der Zuweisungsoperator war falsch, = anstell von === verwendet.






//aktualisierung der objekte auslagern auf klassenmethoden
//todos mit checkox

//delete project
